spring.application.name=ToDo-Backend1

spring.datasource.url=${DB_URL}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.username=${DB_USERNAME}
#spring.datasource.url=jdbc:mysql://localhost:3306/kromann
#spring.datasource.username=ossas
#spring.datasource.password=x

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLDialect
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.messaging=TRACE

# For Spring Security to work with JWT
### Dev / Local
#spring.security.oauth2.resourceserver.jwt.public-key-location=classpath:keys/public.key
#jwt.private-key=classpath:keys/private.pem

### Production -> Need to create a prod application properties
spring.security.oauth2.resourceserver.jwt.public-key-location=file:${JWT_PUBLIC_KEY_PATH}
jwt.private-key=file:${JWT_PRIVATE_KEY_PATH}
